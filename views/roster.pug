extends layout

append head
  title Roster - #{course} - Omnivore

append content
  
  p Roster (#[+plural(roster.length, 'user')])
  
  form(method='post')
    p: textarea.form-control(name='roster', rows=20)= roster.join('\n')
    p: button.btn.btn-default Update
